[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "CFB Payment Flask App"
requires-python = ">=3.11"
dependencies = [
  "flask>=3.0.0",
  "python-dotenv>=1.0.0",
  "requests>=2.31.0"
]
